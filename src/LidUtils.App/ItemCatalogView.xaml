<UserControl x:Class="LidUtils.App.ItemCatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="170"/>
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Padding="8,5"
                     ToolTip="Search display name, definition ID, source table, or category"/>
            <ComboBox Grid.Column="2"
                      ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding SelectedCategory}"
                      Padding="7"
                      ToolTip="Filter by item category"/>
        </Grid>
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}"
                  IsReadOnly="True"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  EnableRowVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding CategoryLabel}" Width="120"/>
                <DataGridTextColumn Header="Definition ID" Binding="{Binding DefinitionId}" Width="150"/>
                <DataGridTextColumn Header="Source table" Binding="{Binding SourceTable}" Width="120"/>
                <DataGridCheckBoxColumn Header="Template" Binding="{Binding IsTemplateSupported}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>
        <Border Grid.Row="4" Padding="10,8" CornerRadius="4" Background="#161B23" BorderBrush="#2A313D" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="{Binding SelectedItemName}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding SelectedItemDetails}" Foreground="#AEB7C6" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
